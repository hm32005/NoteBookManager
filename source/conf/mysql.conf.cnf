[mysqld]

skip-name-resolve

server-id = 1
log_bin = mysql-bin
binlog_format = ROW
binlog_do_db = mydb
gtid-mode=ON
enforce-gtid-consistency=ON
log-replica-updates=ON
binlog-expire-logs-seconds=604800