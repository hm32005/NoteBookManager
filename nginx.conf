upstream notebookservice {
    server notebookservice1:8080;
    server notebookservice2:8080;
    server notebookservice3:8080;
}

server{
	listen 80;
	server_name _;

    location / {
        # resolver 8.8.8.8 ipv6=off;
        proxy_pass http://notebookservice;
    }
}